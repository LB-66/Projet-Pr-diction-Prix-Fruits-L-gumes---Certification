services:
  postgres:
    image: postgres:16
    container_name: fruits_legumes_postgres
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
```

Sauvegarde et ferme.

---

### Fichier 3 — `requirements.txt`

1. Clic droit → **Nouveau → Document texte**
2. Renomme-le **`requirements.txt`**
3. Ouvre-le et colle ceci :
```
pandas
sqlalchemy
psycopg2-binary
python-dotenv